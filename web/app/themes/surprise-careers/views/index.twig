{% extends "base.twig" %}

{% block main %}
{#    <div class="container">#}
    {% for post in posts %}
{#        <h1>{{ post.title }}</h1>#}
        {{ post.content }}
    {% endfor %}
{#    </div>#}
{% endblock %}